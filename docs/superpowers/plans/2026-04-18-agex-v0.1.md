# agex v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v0.1 of the `agex` CLI — a non-agentic Python tool that emits deterministic per-backend markdown for agents, plus a Jekyll docs site and a Claude-Code tester workspace for dogfooding.

**Architecture:** Skill-folder structured Python package (`src/agent_experience/`) with a uniform three-stage pipeline (cli → command script → core renderer). Four backends pluggable via YAML + optional probe. State in cwd-local `.agex/`. See spec: `docs/superpowers/specs/2026-04-18-agex-design.md`.

**Tech Stack:** Python 3.10+, `uv` for deps, `typer` for CLI, `jinja2` for templates, `pyyaml` for YAML, `tomlkit` for TOML (round-trip preserving), `pytest` + `pytest-xdist` + `syrupy` for tests, GitHub Actions for CI, Jekyll + Cloudflare Pages for docs.

---

## Phase 1 — Project scaffolding

### Task 1: Initialize pyproject.toml + uv venv

**Files:**
- Create: `pyproject.toml`
- Create: `.python-version`

- [ ] **Step 1: Write pyproject.toml**

```toml
[project]
name = "agent-experience"
version = "0.1.0"
description = "Agent-operated developer-experience CLI — deterministic per-backend markdown briefings for autonomous agents."
authors = [{name = "Ori Nachum"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.12",
    "jinja2>=3.1",
    "pyyaml>=6.0",
    "tomlkit>=0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-xdist>=3.5",
    "syrupy>=4.6",
    "black>=24.0",
    "isort>=5.13",
    "flake8>=7.0",
    "bandit>=1.7",
]

[project.scripts]
agex = "agent_experience.cli:app"

[project.urls]
Homepage = "https://agent-experience.culture.dev"
Repository = "https://github.com/OriNachum/agent-experience"
Issues = "https://github.com/OriNachum/agent-experience/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_experience"]

[tool.hatch.build.targets.wheel.force-include]
"src/agent_experience/commands" = "agent_experience/commands"
"src/agent_experience/backends" = "agent_experience/backends"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-n auto -v"

[tool.black]
line-length = 100

[tool.isort]
profile = "black"
line_length = 100
```

- [ ] **Step 2: Write .python-version**

```
3.11
```

- [ ] **Step 3: Create venv and install**

Run: `uv venv && uv pip install -e ".[dev]"`
Expected: venv created at `.venv/`, all dev deps installed, no errors.

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml .python-version
git commit -m "chore: init pyproject.toml with uv and dev deps"
```

---

### Task 2: Skeleton package + smoke test

**Files:**
- Create: `src/agent_experience/__init__.py`
- Create: `src/agent_experience/cli.py`
- Create: `tests/__init__.py`
- Create: `tests/test_cli_smoke.py`

- [ ] **Step 1: Write failing smoke test**

File: `tests/test_cli_smoke.py`

```python
from typer.testing import CliRunner
from agent_experience.cli import app


def test_version_flag():
    runner = CliRunner()
    result = runner.invoke(app, ["--version"])
    assert result.exit_code == 0
    assert "0.1.0" in result.stdout
```

- [ ] **Step 2: Run test — expect failure**

Run: `uv run pytest tests/test_cli_smoke.py -v`
Expected: FAIL (module `agent_experience.cli` not found)

- [ ] **Step 3: Write minimal implementation**

File: `src/agent_experience/__init__.py`

```python
__version__ = "0.1.0"
```

File: `src/agent_experience/cli.py`

```python
from typing import Optional

import typer

from agent_experience import __version__

app = typer.Typer(
    name="agex",
    help="Agent-operated developer-experience CLI.",
    no_args_is_help=True,
)


def _version_callback(value: bool) -> None:
    if value:
        typer.echo(__version__)
        raise typer.Exit()


@app.callback()
def main(
    version: Optional[bool] = typer.Option(
        None, "--version", callback=_version_callback, is_eager=True
    ),
) -> None:
    pass
```

File: `tests/__init__.py`

```python
```

- [ ] **Step 4: Run test — expect pass**

Run: `uv run pytest tests/test_cli_smoke.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience tests
git commit -m "feat: add typer CLI skeleton with --version"
```

---

### Task 3: .gitignore and CLAUDE.md

**Files:**
- Create: `.gitignore`
- Create: `CLAUDE.md`

- [ ] **Step 1: Write .gitignore**

```gitignore
# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
*.egg-info/
dist/
build/

# uv
uv.lock

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Project
.agex/data/
coverage.xml
.coverage
htmlcov/
```

- [ ] **Step 2: Write CLAUDE.md**

```markdown
# CLAUDE.md

Guidance for agents working in this repository.

## Project

`agex` — non-agentic Python CLI that emits deterministic per-backend markdown briefings for autonomous agents. PyPI distribution: `agent-experience`. Spec: `docs/superpowers/specs/2026-04-18-agex-design.md`.

## Package Management

`uv` for all Python work. `uv venv && uv pip install -e ".[dev]"` to bootstrap.

## Design Invariants (non-negotiable)

1. Zero LLM calls inside agex. All output is deterministic markdown from Jinja templates.
2. `--agent <backend>` is required on backend-sensitive commands.
3. Side effects only in `gamify`, `gamify --uninstall`, `hook write`, and first-run `.agex/` init.
4. "Unsupported" is success — exit 0 with markdown notice.

## Testing

`uv run pytest` runs everything in parallel. Snapshot tests use `syrupy` — update with `--snapshot-update` only after verifying the new output manually.

## Git Workflow

- Branch for all changes.
- Bump version in `pyproject.toml` and `CHANGELOG.md` before creating a PR.
- Pre-commit hooks run `black`, `isort`, `flake8`, `bandit`.
```

- [ ] **Step 3: Commit**

```bash
git add .gitignore CLAUDE.md
git commit -m "chore: add .gitignore and CLAUDE.md"
```

---

### Task 4: CHANGELOG.md and README.md

**Files:**
- Create: `CHANGELOG.md`
- Modify: `README.md` (currently two-line placeholder)

- [ ] **Step 1: Write CHANGELOG.md**

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial v0.1 scaffolding: pyproject.toml, typer CLI skeleton, CLAUDE.md.
```

- [ ] **Step 2: Rewrite README.md**

```markdown
# agent-experience

Agent-operated developer-experience CLI. Non-agentic, deterministic, markdown-first.

## Install

```bash
uv tool install agent-experience
# or
pipx install agent-experience
```

## Quick start

```bash
agex explain agex
agex overview --agent claude-code
agex learn --agent claude-code
```

## Docs

[agent-experience.culture.dev](https://agent-experience.culture.dev) (coming soon).

Spec: `docs/superpowers/specs/2026-04-18-agex-design.md`.

## License

MIT.
```

- [ ] **Step 3: Commit**

```bash
git add CHANGELOG.md README.md
git commit -m "docs: add CHANGELOG and full README"
```

---

### Task 5: CI test workflow

**Files:**
- Create: `.github/workflows/test.yml`

- [ ] **Step 1: Write workflow**

```yaml
name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: uv venv
      - run: uv pip install -e ".[dev]"
      - run: uv run pytest
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/test.yml
git commit -m "ci: add cross-platform pytest workflow"
```

---

## Phase 2 — Core plumbing

Build `core/` modules one at a time, TDD. Each module has unit tests; later phases consume them.

### Task 6: `core/backend.py` — Backend enum + validation

**Files:**
- Create: `src/agent_experience/core/__init__.py`
- Create: `src/agent_experience/core/backend.py`
- Create: `tests/core/__init__.py`
- Create: `tests/core/test_backend.py`

- [ ] **Step 1: Write failing tests**

File: `tests/core/test_backend.py`

```python
import pytest

from agent_experience.core.backend import Backend, parse_backend


def test_backend_enum_values():
    assert Backend.CLAUDE_CODE.value == "claude-code"
    assert Backend.CODEX.value == "codex"
    assert Backend.COPILOT.value == "copilot"
    assert Backend.ACP.value == "acp"


def test_parse_backend_valid():
    assert parse_backend("claude-code") is Backend.CLAUDE_CODE
    assert parse_backend("codex") is Backend.CODEX


def test_parse_backend_invalid_raises():
    with pytest.raises(ValueError) as exc:
        parse_backend("gemini")
    assert "gemini" in str(exc.value)
    assert "claude-code" in str(exc.value)  # lists valid options


def test_parse_backend_case_sensitive():
    with pytest.raises(ValueError):
        parse_backend("Claude-Code")
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/core/test_backend.py -v`
Expected: FAIL (module not found)

- [ ] **Step 3: Implement**

File: `src/agent_experience/core/__init__.py`

```python
```

File: `src/agent_experience/core/backend.py`

```python
from enum import Enum


class Backend(str, Enum):
    CLAUDE_CODE = "claude-code"
    CODEX = "codex"
    COPILOT = "copilot"
    ACP = "acp"


def parse_backend(value: str) -> Backend:
    try:
        return Backend(value)
    except ValueError:
        valid = ", ".join(b.value for b in Backend)
        raise ValueError(f"unknown backend '{value}' (one of: {valid})") from None
```

File: `tests/core/__init__.py`

```python
```

- [ ] **Step 4: Run — expect pass**

Run: `uv run pytest tests/core/test_backend.py -v`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/core tests/core
git commit -m "feat(core): add Backend enum and parse_backend"
```

---

### Task 7: `core/paths.py` — `.agex/` discovery and init

**Files:**
- Create: `src/agent_experience/core/paths.py`
- Create: `tests/core/test_paths.py`

- [ ] **Step 1: Write failing tests**

File: `tests/core/test_paths.py`

```python
from pathlib import Path

from agent_experience.core.paths import agex_dir, config_path, data_dir, ensure_init


def test_agex_dir_uses_cwd(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    assert agex_dir() == tmp_path / ".agex"


def test_ensure_init_creates_dir_and_gitignore(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    assert (tmp_path / ".agex").is_dir()
    assert (tmp_path / ".agex" / "data").is_dir()
    gi = tmp_path / ".agex" / ".gitignore"
    assert gi.exists()
    assert "data/" in gi.read_text()


def test_ensure_init_is_idempotent(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    (tmp_path / ".agex" / "existing.txt").write_text("keep me")
    ensure_init()
    assert (tmp_path / ".agex" / "existing.txt").read_text() == "keep me"


def test_config_path_and_data_dir(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    assert config_path() == tmp_path / ".agex" / "config.toml"
    assert data_dir() == tmp_path / ".agex" / "data"
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/core/test_paths.py -v`
Expected: FAIL

- [ ] **Step 3: Implement**

File: `src/agent_experience/core/paths.py`

```python
from pathlib import Path

GITIGNORE_CONTENT = "# Managed by agex — do not edit.\ndata/\n"


def agex_dir(cwd: Path | None = None) -> Path:
    base = cwd if cwd is not None else Path.cwd()
    return base / ".agex"


def config_path(cwd: Path | None = None) -> Path:
    return agex_dir(cwd) / "config.toml"


def data_dir(cwd: Path | None = None) -> Path:
    return agex_dir(cwd) / "data"


def ensure_init(cwd: Path | None = None) -> Path:
    root = agex_dir(cwd)
    root.mkdir(parents=True, exist_ok=True)
    data_dir(cwd).mkdir(exist_ok=True)
    gi = root / ".gitignore"
    if not gi.exists():
        gi.write_text(GITIGNORE_CONTENT)
    return root
```

- [ ] **Step 4: Run — expect pass**

Run: `uv run pytest tests/core/test_paths.py -v`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/core/paths.py tests/core/test_paths.py
git commit -m "feat(core): add .agex/ discovery and idempotent init"
```

---

### Task 8: `core/config.py` — TOML round-trip

**Files:**
- Create: `src/agent_experience/core/config.py`
- Create: `tests/core/test_config.py`

- [ ] **Step 1: Write failing tests**

File: `tests/core/test_config.py`

```python
from agent_experience.core.config import Config, load, save
from agent_experience.core.paths import ensure_init


def test_load_returns_empty_when_missing(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    cfg = load()
    assert cfg.agex_version == "0.1.0"
    assert cfg.installed == {}
    assert cfg.preferences == {}


def test_save_then_load_roundtrip(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    cfg = Config(agex_version="0.1.0")
    cfg.installed["gamify"] = {
        "at": "2026-04-18T10:00:00Z",
        "hook_fragment_ids": ["agex:post-tool-use"],
    }
    save(cfg)
    reloaded = load()
    assert reloaded.installed["gamify"]["hook_fragment_ids"] == ["agex:post-tool-use"]
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/core/test_config.py -v`
Expected: FAIL

- [ ] **Step 3: Implement**

File: `src/agent_experience/core/config.py`

```python
from dataclasses import dataclass, field
from typing import Any

import tomlkit

from agent_experience.core.paths import config_path


@dataclass
class Config:
    agex_version: str = "0.1.0"
    backend: str | None = None
    installed: dict[str, dict[str, Any]] = field(default_factory=dict)
    preferences: dict[str, Any] = field(default_factory=dict)


def load() -> Config:
    path = config_path()
    if not path.exists():
        return Config()
    doc = tomlkit.parse(path.read_text())
    return Config(
        agex_version=doc.get("agex_version", "0.1.0"),
        backend=doc.get("backend"),
        installed=dict(doc.get("installed", {})),
        preferences=dict(doc.get("preferences", {})),
    )


def save(cfg: Config) -> None:
    doc = tomlkit.document()
    doc["agex_version"] = cfg.agex_version
    if cfg.backend is not None:
        doc["backend"] = cfg.backend
    if cfg.installed:
        doc["installed"] = cfg.installed
    if cfg.preferences:
        doc["preferences"] = cfg.preferences
    config_path().write_text(tomlkit.dumps(doc))
```

- [ ] **Step 4: Run — expect pass**

Run: `uv run pytest tests/core/test_config.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/core/config.py tests/core/test_config.py
git commit -m "feat(core): add Config dataclass with TOML round-trip"
```

---

### Task 9: `core/render.py` — Jinja2 renderer

**Files:**
- Create: `src/agent_experience/core/render.py`
- Create: `tests/core/test_render.py`

- [ ] **Step 1: Write failing tests**

File: `tests/core/test_render.py`

```python
import pytest

from agent_experience.core.render import render_string, render_file


def test_render_string_injects_context():
    out = render_string("Hello, {{ name }}!", {"name": "agex"})
    assert out == "Hello, agex!"


def test_render_string_strict_undefined():
    with pytest.raises(Exception) as exc:
        render_string("Hello, {{ missing }}!", {})
    assert "missing" in str(exc.value)


def test_render_file_reads_and_renders(tmp_path):
    tmpl = tmp_path / "t.md.j2"
    tmpl.write_text("# {{ title }}")
    out = render_file(tmpl, {"title": "agex"})
    assert out == "# agex"
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/core/test_render.py -v`
Expected: FAIL

- [ ] **Step 3: Implement**

File: `src/agent_experience/core/render.py`

```python
from pathlib import Path
from typing import Any

from jinja2 import Environment, FileSystemLoader, StrictUndefined

_ENV = Environment(
    loader=FileSystemLoader("."),
    undefined=StrictUndefined,
    keep_trailing_newline=True,
)


def render_string(template: str, context: dict[str, Any]) -> str:
    return _ENV.from_string(template).render(**context)


def render_file(path: Path, context: dict[str, Any]) -> str:
    return render_string(path.read_text(), context)
```

- [ ] **Step 4: Run — expect pass**

Run: `uv run pytest tests/core/test_render.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/core/render.py tests/core/test_render.py
git commit -m "feat(core): add Jinja2 renderer with StrictUndefined"
```

---

### Task 10: `core/skill_loader.py` — SKILL.md parser

**Files:**
- Create: `src/agent_experience/core/skill_loader.py`
- Create: `tests/core/test_skill_loader.py`

- [ ] **Step 1: Write failing tests**

File: `tests/core/test_skill_loader.py`

```python
import pytest

from agent_experience.core.skill_loader import Skill, load_skill


def test_load_skill_parses_frontmatter_and_body(tmp_path):
    path = tmp_path / "SKILL.md"
    path.write_text(
        "---\n"
        "name: overview\n"
        "description: Snapshot of agent setup.\n"
        "type: command\n"
        "---\n"
        "\n"
        "# Overview body\n"
    )
    skill = load_skill(path)
    assert skill.name == "overview"
    assert skill.description == "Snapshot of agent setup."
    assert skill.type == "command"
    assert "# Overview body" in skill.body


def test_load_skill_missing_frontmatter_raises(tmp_path):
    path = tmp_path / "SKILL.md"
    path.write_text("# No frontmatter\n")
    with pytest.raises(ValueError) as exc:
        load_skill(path)
    assert "frontmatter" in str(exc.value).lower()


def test_load_skill_missing_required_field_raises(tmp_path):
    path = tmp_path / "SKILL.md"
    path.write_text("---\nname: x\n---\nbody\n")
    with pytest.raises(ValueError) as exc:
        load_skill(path)
    assert "description" in str(exc.value)
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/core/test_skill_loader.py -v`
Expected: FAIL

- [ ] **Step 3: Implement**

File: `src/agent_experience/core/skill_loader.py`

```python
import re
from dataclasses import dataclass
from pathlib import Path

import yaml

_FRONTMATTER_RE = re.compile(r"\A---\n(.*?)\n---\n(.*)\Z", re.DOTALL)

REQUIRED_FIELDS = ("name", "description", "type")


@dataclass
class Skill:
    name: str
    description: str
    type: str
    body: str
    path: Path


def load_skill(path: Path) -> Skill:
    text = path.read_text()
    match = _FRONTMATTER_RE.match(text)
    if not match:
        raise ValueError(f"{path}: missing YAML frontmatter")
    meta = yaml.safe_load(match.group(1)) or {}
    for field_name in REQUIRED_FIELDS:
        if field_name not in meta:
            raise ValueError(f"{path}: frontmatter missing required field '{field_name}'")
    return Skill(
        name=meta["name"],
        description=meta["description"],
        type=meta["type"],
        body=match.group(2).lstrip("\n"),
        path=path,
    )
```

- [ ] **Step 4: Run — expect pass**

Run: `uv run pytest tests/core/test_skill_loader.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/core/skill_loader.py tests/core/test_skill_loader.py
git commit -m "feat(core): add SKILL.md frontmatter + body parser"
```

---

### Task 11: `core/capabilities.py` — capability matrix + unsupported notices

**Files:**
- Create: `src/agent_experience/core/capabilities.py`
- Create: `tests/core/test_capabilities.py`

- [ ] **Step 1: Write failing tests**

File: `tests/core/test_capabilities.py`

```python
from agent_experience.core.backend import Backend
from agent_experience.core.capabilities import (
    CapabilityMatrix,
    is_supported,
    unsupported_notice,
)


def test_is_supported_true(tmp_path):
    yaml_path = tmp_path / "claude-code.yaml"
    yaml_path.write_text("hooks: true\nmcp: true\n")
    matrix = CapabilityMatrix.load({Backend.CLAUDE_CODE: yaml_path})
    assert is_supported(matrix, Backend.CLAUDE_CODE, "hooks")


def test_is_supported_false(tmp_path):
    yaml_path = tmp_path / "acp.yaml"
    yaml_path.write_text("hooks: false\nmcp: true\n")
    matrix = CapabilityMatrix.load({Backend.ACP: yaml_path})
    assert not is_supported(matrix, Backend.ACP, "hooks")


def test_unsupported_notice_renders_markdown(tmp_path):
    yaml_path = tmp_path / "acp.yaml"
    yaml_path.write_text(
        "hooks: false\n"
        "hooks_alternative: |\n"
        "  Wrap your agent invocation in a shell script that logs events manually.\n"
    )
    matrix = CapabilityMatrix.load({Backend.ACP: yaml_path})
    notice = unsupported_notice(matrix, Backend.ACP, "hooks")
    assert "not supported on acp" in notice.lower()
    assert "Wrap your agent invocation" in notice
    assert "https://github.com/OriNachum/agent-experience/issues" in notice
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/core/test_capabilities.py -v`
Expected: FAIL

- [ ] **Step 3: Implement**

File: `src/agent_experience/core/capabilities.py`

```python
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any

import yaml

from agent_experience.core.backend import Backend

ISSUE_URL = "https://github.com/OriNachum/agent-experience/issues"


@dataclass
class CapabilityMatrix:
    data: dict[Backend, dict[str, Any]] = field(default_factory=dict)

    @classmethod
    def load(cls, sources: dict[Backend, Path]) -> "CapabilityMatrix":
        matrix: dict[Backend, dict[str, Any]] = {}
        for backend, path in sources.items():
            matrix[backend] = yaml.safe_load(path.read_text()) or {}
        return cls(matrix)


def is_supported(matrix: CapabilityMatrix, backend: Backend, capability: str) -> bool:
    return bool(matrix.data.get(backend, {}).get(capability, False))


def unsupported_notice(matrix: CapabilityMatrix, backend: Backend, capability: str) -> str:
    data = matrix.data.get(backend, {})
    alt = data.get(f"{capability}_alternative", "").strip()
    lines = [
        f"## `{capability}` is not supported on {backend.value}",
        "",
    ]
    if alt:
        lines.append("**Closest alternative:**")
        lines.append("")
        lines.append(alt)
        lines.append("")
    lines.append(
        f"Want `{capability}` supported on {backend.value}, or have a better alternative? "
        f"Open an issue: <{ISSUE_URL}>"
    )
    return "\n".join(lines) + "\n"
```

- [ ] **Step 4: Run — expect pass**

Run: `uv run pytest tests/core/test_capabilities.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/core/capabilities.py tests/core/test_capabilities.py
git commit -m "feat(core): add capability matrix + unsupported-notice renderer"
```

---

### Task 12: `core/hook_io.py` — JSON append + markdown table render

**Files:**
- Create: `src/agent_experience/core/hook_io.py`
- Create: `tests/core/test_hook_io.py`

- [ ] **Step 1: Write failing tests**

File: `tests/core/test_hook_io.py`

```python
from concurrent.futures import ThreadPoolExecutor

from agent_experience.core.hook_io import append_event, load_events, render_table
from agent_experience.core.paths import ensure_init


def test_append_and_load_roundtrip(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    append_event("post-tool-use", {"tool": "Read", "ts": "2026-04-18T10:00:00Z"})
    append_event("post-tool-use", {"tool": "Write", "ts": "2026-04-18T10:00:01Z"})
    events = load_events("post-tool-use")
    assert len(events) == 2
    assert events[0]["tool"] == "Read"


def test_load_events_missing_stream_returns_empty(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    assert load_events("never-written") == []


def test_render_table_produces_markdown():
    events = [
        {"ts": "2026-04-18T10:00:00Z", "tool": "Read"},
        {"ts": "2026-04-18T10:00:01Z", "tool": "Write"},
    ]
    table = render_table(events, columns=["ts", "tool"])
    assert "| ts | tool |" in table
    assert "| 2026-04-18T10:00:00Z | Read |" in table


def test_concurrent_appends_no_corruption(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    ensure_init()
    with ThreadPoolExecutor(max_workers=8) as ex:
        list(ex.map(
            lambda i: append_event("stop", {"i": i}),
            range(50),
        ))
    events = load_events("stop")
    assert len(events) == 50
    assert sorted(e["i"] for e in events) == list(range(50))
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/core/test_hook_io.py -v`
Expected: FAIL

- [ ] **Step 3: Implement**

File: `src/agent_experience/core/hook_io.py`

```python
import json
import os
import sys
from pathlib import Path
from typing import Any

from agent_experience.core.paths import data_dir

if sys.platform == "win32":
    import msvcrt

    def _lock(fh) -> None:
        msvcrt.locking(fh.fileno(), msvcrt.LK_LOCK, 1)

    def _unlock(fh) -> None:
        msvcrt.locking(fh.fileno(), msvcrt.LK_UNLCK, 1)

else:
    import fcntl

    def _lock(fh) -> None:
        fcntl.flock(fh.fileno(), fcntl.LOCK_EX)

    def _unlock(fh) -> None:
        fcntl.flock(fh.fileno(), fcntl.LOCK_UN)


def _stream_path(stream: str) -> Path:
    return data_dir() / f"{stream}.json"


def append_event(stream: str, payload: dict[str, Any]) -> None:
    path = _stream_path(stream)
    path.parent.mkdir(parents=True, exist_ok=True)
    line = json.dumps(payload, separators=(",", ":")) + "\n"
    with path.open("a", encoding="utf-8") as fh:
        _lock(fh)
        try:
            fh.write(line)
            fh.flush()
            os.fsync(fh.fileno())
        finally:
            _unlock(fh)


def load_events(stream: str) -> list[dict[str, Any]]:
    path = _stream_path(stream)
    if not path.exists():
        return []
    events: list[dict[str, Any]] = []
    for line in path.read_text(encoding="utf-8").splitlines():
        if line.strip():
            events.append(json.loads(line))
    return events


def render_table(events: list[dict[str, Any]], columns: list[str]) -> str:
    header = "| " + " | ".join(columns) + " |"
    sep = "| " + " | ".join("---" for _ in columns) + " |"
    rows = ["| " + " | ".join(str(e.get(c, "")) for c in columns) + " |" for e in events]
    return "\n".join([header, sep, *rows]) + "\n"
```

> **Note:** file is stored as JSON-lines (one object per line) even though extension is `.json` — keeps append fast and avoids rewriting whole file.

- [ ] **Step 4: Run — expect pass**

Run: `uv run pytest tests/core/test_hook_io.py -v`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/core/hook_io.py tests/core/test_hook_io.py
git commit -m "feat(core): add hook JSON append/load with cross-platform file lock"
```

---

## Phase 3 — First vertical slice: `agex explain`

Simplest command; no backend probe needed. Establishes the skill-folder pattern.

### Task 13: `commands/explain/` skill-folder scaffolding

**Files:**
- Create: `src/agent_experience/commands/__init__.py`
- Create: `src/agent_experience/commands/explain/__init__.py`
- Create: `src/agent_experience/commands/explain/SKILL.md`
- Create: `src/agent_experience/commands/explain/scripts/__init__.py`
- Create: `src/agent_experience/commands/explain/references/.gitkeep`
- Create: `src/agent_experience/commands/explain/assets/topics/agex.md`

- [ ] **Step 1: Write explain's own SKILL.md**

File: `src/agent_experience/commands/explain/SKILL.md`

```markdown
---
name: explain
description: Emit markdown documentation for any agex command, lesson, or concept.
type: command
---

# `agex explain <topic>`

Use this to get authoritative, deterministic documentation on an agex command, lesson, or concept without invoking a lesson or running a probe.

## How it resolves

1. `commands/<topic>/SKILL.md` (command-level, wins if present)
2. `commands/learn/assets/topics/<topic>/SKILL.md` (lesson-level)
3. `commands/explain/assets/topics/<topic>.md` (concept-level override)

First match wins.

## From your shell tool

```bash
agex explain overview
agex explain gamify
agex explain levelup
agex explain agex          # self-describing page
```
```

- [ ] **Step 2: Write `agex` self-describing topic**

File: `src/agent_experience/commands/explain/assets/topics/agex.md`

```markdown
# `agex` — agent-operated developer-experience CLI

`agex` is a non-agentic Python CLI that emits deterministic per-backend markdown for autonomous agents. You (the agent) invoke it from your shell tool to learn about and configure your own runtime.

## Commands

| Command | Purpose |
|---|---|
| `agex overview --agent X` | Snapshot of the project's current setup for backend X. |
| `agex learn --agent X` | Menu of lesson topics available for backend X. |
| `agex learn <topic> --agent X` | Teach a lesson (e.g., introspect, visualize, gamify, levelup). |
| `agex gamify --agent X` | Install usage-tracking hooks (or unsupported notice). |
| `agex gamify --uninstall --agent X` | Reverse `gamify`. |
| `agex hook write <event> [...]` | Append a tracking event. Called by installed hooks. |
| `agex hook read --agent X` | Show tracked events as markdown + source path. |
| `agex explain <topic>` | You're reading this. |

## First steps

```bash
agex explain agex              # this page
agex learn --agent claude-code  # what can I learn for my backend?
agex overview --agent claude-code  # what's in this project?
```

## Design invariants

- **Non-agentic.** Zero LLM calls inside agex. All output is deterministic.
- **Markdown is the universal format.** No `--json` flag.
- **`--agent` is required** on backend-sensitive commands.
- **Unsupported is success.** If your backend lacks a feature, you get a markdown notice + link to file an issue — exit code 0.

## Repo

<https://github.com/OriNachum/agent-experience>
```

- [ ] **Step 3: Write empty placeholder files**

```bash
touch src/agent_experience/commands/__init__.py
touch src/agent_experience/commands/explain/__init__.py
touch src/agent_experience/commands/explain/scripts/__init__.py
mkdir -p src/agent_experience/commands/explain/references
touch src/agent_experience/commands/explain/references/.gitkeep
```

- [ ] **Step 4: Commit**

```bash
git add src/agent_experience/commands
git commit -m "feat(explain): scaffold skill-folder with SKILL.md and agex topic"
```

---

### Task 14: `explain.py` script + CLI wiring + tests

**Files:**
- Create: `src/agent_experience/commands/explain/scripts/explain.py`
- Modify: `src/agent_experience/cli.py`
- Create: `tests/commands/__init__.py`
- Create: `tests/commands/test_explain.py`

- [ ] **Step 1: Write failing tests**

File: `tests/commands/test_explain.py`

```python
from typer.testing import CliRunner

from agent_experience.cli import app


def test_explain_agex_prints_self_describing_page():
    runner = CliRunner()
    result = runner.invoke(app, ["explain", "agex"])
    assert result.exit_code == 0
    assert "agex" in result.stdout
    assert "overview" in result.stdout
    assert "learn" in result.stdout


def test_explain_explain_reads_command_skill_md():
    runner = CliRunner()
    result = runner.invoke(app, ["explain", "explain"])
    assert result.exit_code == 0
    assert "agex explain" in result.stdout.lower()


def test_explain_unknown_topic_exits_2_with_menu():
    runner = CliRunner()
    result = runner.invoke(app, ["explain", "unknown-topic-xyz"])
    assert result.exit_code == 2
    assert "unknown" in result.stderr.lower()
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/commands/test_explain.py -v`
Expected: FAIL (command not wired)

- [ ] **Step 3: Implement script**

File: `src/agent_experience/commands/explain/scripts/explain.py`

```python
from importlib.resources import files
from pathlib import Path

from agent_experience.core.skill_loader import load_skill


def _commands_root() -> Path:
    return Path(str(files("agent_experience.commands")))


def resolve_topic(topic: str) -> tuple[str, Path] | None:
    """Resolve topic per spec precedence. Returns (kind, path) or None."""
    cmds = _commands_root()

    cmd_skill = cmds / topic / "SKILL.md"
    if cmd_skill.exists():
        return ("command", cmd_skill)

    lesson_skill = cmds / "learn" / "assets" / "topics" / topic / "SKILL.md"
    if lesson_skill.exists():
        return ("lesson", lesson_skill)

    concept = cmds / "explain" / "assets" / "topics" / f"{topic}.md"
    if concept.exists():
        return ("concept", concept)

    return None


def run(topic: str) -> tuple[str, int, str]:
    """Return (stdout, exit_code, stderr)."""
    resolved = resolve_topic(topic)
    if resolved is None:
        agex_page = _commands_root() / "explain" / "assets" / "topics" / "agex.md"
        body = agex_page.read_text() if agex_page.exists() else ""
        return (body, 2, f"agex: error: unknown topic '{topic}'")

    kind, path = resolved
    if kind == "concept":
        return (path.read_text(), 0, "")
    skill = load_skill(path)
    return (skill.body, 0, "")
```

- [ ] **Step 4: Wire into CLI**

Modify: `src/agent_experience/cli.py`

```python
import sys
from typing import Optional

import typer

from agent_experience import __version__
from agent_experience.commands.explain.scripts import explain as explain_script

app = typer.Typer(
    name="agex",
    help="Agent-operated developer-experience CLI.",
    no_args_is_help=True,
)


def _version_callback(value: bool) -> None:
    if value:
        typer.echo(__version__)
        raise typer.Exit()


@app.callback()
def main(
    version: Optional[bool] = typer.Option(
        None, "--version", callback=_version_callback, is_eager=True
    ),
) -> None:
    pass


@app.command("explain")
def explain(topic: str = typer.Argument(..., help="Topic to explain.")) -> None:
    stdout, exit_code, stderr = explain_script.run(topic)
    if stdout:
        typer.echo(stdout, nl=False)
    if stderr:
        typer.echo(stderr, err=True)
    if exit_code != 0:
        raise typer.Exit(code=exit_code)
```

- [ ] **Step 5: Run — expect pass**

Run: `uv run pytest tests/commands/test_explain.py -v`
Expected: PASS (3 tests)

File: `tests/commands/__init__.py`

```python
```

- [ ] **Step 6: Commit**

```bash
git add src/agent_experience/cli.py \
        src/agent_experience/commands/explain/scripts/explain.py \
        tests/commands
git commit -m "feat(explain): wire agex explain with precedence resolution"
```

---

## Phase 4 — `overview` + Claude Code probe

### Task 15: `backends/claude_code/probe.py` — probe Claude Code state

**Files:**
- Create: `src/agent_experience/backends/__init__.py`
- Create: `src/agent_experience/backends/claude_code/__init__.py`
- Create: `src/agent_experience/backends/claude_code/probe.py`
- Create: `tests/backends/__init__.py`
- Create: `tests/backends/test_claude_code_probe.py`
- Create: `tests/fixtures/claude-code/empty/.gitkeep`
- Create: `tests/fixtures/claude-code/typical/.claude/settings.json`
- Create: `tests/fixtures/claude-code/typical/CLAUDE.md`
- Create: `tests/fixtures/claude-code/typical/.claude/skills/example/SKILL.md`

- [ ] **Step 1: Write fixture tree**

File: `tests/fixtures/claude-code/typical/.claude/settings.json`

```json
{
  "permissions": {
    "allow": ["Bash"]
  }
}
```

File: `tests/fixtures/claude-code/typical/CLAUDE.md`

```markdown
# CLAUDE.md

Test fixture project.
```

File: `tests/fixtures/claude-code/typical/.claude/skills/example/SKILL.md`

```markdown
---
name: example
description: Example skill.
type: command
---
body
```

- [ ] **Step 2: Write failing tests**

File: `tests/backends/test_claude_code_probe.py`

```python
from pathlib import Path

from agent_experience.backends.claude_code.probe import probe


FIXTURES = Path(__file__).parent.parent / "fixtures" / "claude-code"


def test_probe_empty_project():
    result = probe(FIXTURES / "empty")
    assert result.skills == []
    assert result.hooks == []
    assert result.claude_md is None


def test_probe_typical_project():
    result = probe(FIXTURES / "typical")
    assert len(result.skills) == 1
    assert result.skills[0]["name"] == "example"
    assert result.claude_md is not None
    assert "CLAUDE.md" in str(result.claude_md)


def test_probe_missing_directory_returns_empty():
    result = probe(Path("/nonexistent-path-abc-123"))
    assert result.skills == []
```

- [ ] **Step 3: Run — expect fail**

Run: `uv run pytest tests/backends/test_claude_code_probe.py -v`
Expected: FAIL (module missing)

- [ ] **Step 4: Implement**

File: `src/agent_experience/backends/__init__.py`

```python
```

File: `src/agent_experience/backends/claude_code/__init__.py`

```python
```

File: `src/agent_experience/backends/claude_code/probe.py`

```python
import json
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any


@dataclass
class ProbeResult:
    skills: list[dict[str, Any]] = field(default_factory=list)
    hooks: list[dict[str, Any]] = field(default_factory=list)
    agents: list[dict[str, Any]] = field(default_factory=list)
    mcp_servers: list[dict[str, Any]] = field(default_factory=list)
    claude_md: Path | None = None
    settings: dict[str, Any] | None = None
    warnings: list[str] = field(default_factory=list)


def _read_skill(path: Path) -> dict[str, Any] | None:
    try:
        text = path.read_text()
        # Cheap frontmatter name/description extraction (skill_loader would raise on malformed)
        import yaml
        import re
        m = re.match(r"\A---\n(.*?)\n---\n", text, re.DOTALL)
        if not m:
            return None
        meta = yaml.safe_load(m.group(1)) or {}
        return {
            "name": meta.get("name", path.parent.name),
            "description": meta.get("description", ""),
            "path": str(path),
        }
    except Exception:
        return None


def probe(project_dir: Path) -> ProbeResult:
    result = ProbeResult()
    if not project_dir.exists():
        return result

    claude_md = project_dir / "CLAUDE.md"
    if claude_md.exists():
        result.claude_md = claude_md

    settings = project_dir / ".claude" / "settings.json"
    if settings.exists():
        try:
            result.settings = json.loads(settings.read_text())
        except json.JSONDecodeError as e:
            result.warnings.append(f"could not parse {settings}: {e}")

    skills_dir = project_dir / ".claude" / "skills"
    if skills_dir.is_dir():
        for skill_md in skills_dir.glob("*/SKILL.md"):
            parsed = _read_skill(skill_md)
            if parsed:
                result.skills.append(parsed)
            else:
                result.warnings.append(f"could not parse {skill_md}")

    hooks_file = project_dir / ".claude" / "hooks.json"
    if hooks_file.exists():
        try:
            data = json.loads(hooks_file.read_text())
            for event, entries in data.items():
                result.hooks.append({"event": event, "entries": entries})
        except json.JSONDecodeError as e:
            result.warnings.append(f"could not parse {hooks_file}: {e}")

    return result
```

File: `tests/backends/__init__.py`

```python
```

- [ ] **Step 5: Run — expect pass**

Run: `uv run pytest tests/backends/test_claude_code_probe.py -v`
Expected: PASS (3 tests)

- [ ] **Step 6: Commit**

```bash
git add src/agent_experience/backends tests/backends tests/fixtures/claude-code
git commit -m "feat(backends): add claude-code probe with ProbeResult"
```

---

### Task 16: `commands/overview/` skill-folder + template + Claude-Code assets

**Files:**
- Create: `src/agent_experience/commands/overview/__init__.py`
- Create: `src/agent_experience/commands/overview/SKILL.md`
- Create: `src/agent_experience/commands/overview/scripts/__init__.py`
- Create: `src/agent_experience/commands/overview/references/.gitkeep`
- Create: `src/agent_experience/commands/overview/assets/sections.md.j2`
- Create: `src/agent_experience/commands/overview/assets/backends/claude-code.yaml`

- [ ] **Step 1: Write SKILL.md**

File: `src/agent_experience/commands/overview/SKILL.md`

```markdown
---
name: overview
description: Render a descriptive markdown snapshot of the project's current agent setup.
type: command
---

# `agex overview --agent <backend>`

Call this to get a read-only markdown snapshot of what's configured in the current project for a given backend — skills, hooks, agents, MCP servers, and relevant config files. Descriptive, not diagnostic. Read it before you act.

## From your shell tool

```bash
agex overview --agent claude-code
agex overview --agent codex
```

## What you get

Markdown sections: Project root, `CLAUDE.md`/`AGENTS.md` presence, Skills, Hooks, Agents, MCP servers, Settings.

## Notes

- Unknown fields are skipped with a `> ⚠️` inline warning.
- Read-only except first-run `.agex/` init.
- Build diagnostic logic (gaps, recommendations) into an agent-authored skill: `agex learn introspect --agent <backend>`.
```

- [ ] **Step 2: Write Jinja template**

File: `src/agent_experience/commands/overview/assets/sections.md.j2`

```jinja
# Overview — {{ backend }}

**Project:** `{{ project_dir }}`

{% if probe.warnings %}
{%- for w in probe.warnings %}
> ⚠️ {{ w }}
{%- endfor %}

{% endif %}
## Project memory

{% if probe.claude_md %}
- `{{ probe.claude_md.name }}` present at `{{ probe.claude_md }}`
{% else %}
- _no CLAUDE.md / AGENTS.md equivalent found_
{% endif %}

## Skills ({{ probe.skills|length }})

{% if probe.skills %}
| name | description |
|---|---|
{%- for s in probe.skills %}
| `{{ s.name }}` | {{ s.description }} |
{%- endfor %}
{% else %}
_none_
{% endif %}

## Hooks ({{ probe.hooks|length }})

{% if probe.hooks %}
{%- for h in probe.hooks %}
- `{{ h.event }}` — {{ h.entries|length }} entry(s)
{%- endfor %}
{% else %}
_none_
{% endif %}

## MCP servers ({{ probe.mcp_servers|length }})

{% if probe.mcp_servers %}
{%- for m in probe.mcp_servers %}
- `{{ m.name }}`
{%- endfor %}
{% else %}
_none_
{% endif %}

## Settings

{% if probe.settings %}
```json
{{ probe.settings | tojson(indent=2) }}
```
{% else %}
_no settings file found_
{% endif %}
```

- [ ] **Step 3: Write Claude Code backend config**

File: `src/agent_experience/commands/overview/assets/backends/claude-code.yaml`

```yaml
sections:
  - project_memory
  - skills
  - hooks
  - mcp
  - settings
probe: claude_code
```

- [ ] **Step 4: Write `__init__.py` and `references/.gitkeep`**

```bash
touch src/agent_experience/commands/overview/__init__.py
touch src/agent_experience/commands/overview/scripts/__init__.py
mkdir -p src/agent_experience/commands/overview/references
touch src/agent_experience/commands/overview/references/.gitkeep
```

- [ ] **Step 5: Commit**

```bash
git add src/agent_experience/commands/overview
git commit -m "feat(overview): scaffold skill-folder with SKILL.md, template, claude-code yaml"
```

---

### Task 17: `overview.py` script + CLI wiring + snapshot tests

**Files:**
- Create: `src/agent_experience/commands/overview/scripts/overview.py`
- Modify: `src/agent_experience/cli.py`
- Create: `tests/commands/test_overview.py`

- [ ] **Step 1: Write failing tests**

Fixtures are read-only; each test copies the relevant fixture tree into `tmp_path` before running so `.agex/` init doesn't dirty the repo.

File: `tests/commands/test_overview.py`

```python
import shutil
from pathlib import Path

from typer.testing import CliRunner

from agent_experience.cli import app


FIXTURES = Path(__file__).parent.parent / "fixtures" / "claude-code"


def _copy_fixture(name: str, tmp_path: Path) -> Path:
    dest = tmp_path / "project"
    shutil.copytree(FIXTURES / name, dest)
    return dest


def test_overview_typical_project(tmp_path, monkeypatch):
    project = _copy_fixture("typical", tmp_path)
    monkeypatch.chdir(project)
    runner = CliRunner()
    result = runner.invoke(app, ["overview", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "# Overview — claude-code" in result.stdout
    assert "example" in result.stdout
    assert "CLAUDE.md" in result.stdout
    assert "## Skills (1)" in result.stdout


def test_overview_empty_project(tmp_path, monkeypatch):
    project = _copy_fixture("empty", tmp_path)
    monkeypatch.chdir(project)
    runner = CliRunner()
    result = runner.invoke(app, ["overview", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "_none_" in result.stdout
    assert "no CLAUDE.md" in result.stdout


def test_overview_missing_agent_flag_errors(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    result = runner.invoke(app, ["overview"])
    assert result.exit_code != 0
    assert "agent" in (result.stderr + result.stdout).lower()


def test_overview_invalid_agent_errors(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    result = runner.invoke(app, ["overview", "--agent", "gemini"])
    assert result.exit_code == 2
    assert "gemini" in result.stderr
```

The `empty` fixture needs a `.gitkeep` to exist as a directory in git; `.claude/skills` is absent, so the probe returns empty lists and the template emits `_none_` + `no CLAUDE.md...` blocks.

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/commands/test_overview.py -v`
Expected: FAIL (command not wired)

- [ ] **Step 3: Implement script**

File: `src/agent_experience/commands/overview/scripts/overview.py`

```python
from importlib.resources import files
from pathlib import Path

from agent_experience.backends.claude_code.probe import probe as claude_code_probe
from agent_experience.core.backend import Backend
from agent_experience.core.paths import ensure_init
from agent_experience.core.render import render_file


_PROBES = {
    Backend.CLAUDE_CODE: claude_code_probe,
}


def _assets_root() -> Path:
    return Path(str(files("agent_experience.commands.overview.assets")))


def run(backend: Backend) -> tuple[str, int, str]:
    ensure_init()
    project_dir = Path.cwd()

    if backend not in _PROBES:
        # Other backends land here until their probes ship; emit empty probe.
        from agent_experience.backends.claude_code.probe import ProbeResult
        probe_result = ProbeResult()
    else:
        probe_result = _PROBES[backend](project_dir)

    template = _assets_root() / "sections.md.j2"
    out = render_file(
        template,
        {"backend": backend.value, "project_dir": project_dir, "probe": probe_result},
    )
    return (out, 0, "")
```

- [ ] **Step 4: Wire into CLI**

Modify: `src/agent_experience/cli.py` — add after the `explain` command:

```python
from agent_experience.core.backend import Backend, parse_backend
from agent_experience.commands.overview.scripts import overview as overview_script


def _agent_option():
    return typer.Option(..., "--agent", help="Backend: claude-code, codex, copilot, or acp.")


@app.command("overview")
def overview(agent: str = _agent_option()) -> None:
    try:
        backend = parse_backend(agent)
    except ValueError as e:
        typer.echo(f"agex: error: {e}", err=True)
        raise typer.Exit(code=2)
    stdout, exit_code, stderr = overview_script.run(backend)
    if stdout:
        typer.echo(stdout, nl=False)
    if stderr:
        typer.echo(stderr, err=True)
    if exit_code != 0:
        raise typer.Exit(code=exit_code)
```

- [ ] **Step 5: Run — expect pass**

Run: `uv run pytest tests/commands/test_overview.py -v`
Expected: PASS (4 tests)

- [ ] **Step 6: Commit**

```bash
git add src/agent_experience/cli.py \
        src/agent_experience/commands/overview/scripts/overview.py \
        tests/commands/test_overview.py
git commit -m "feat(overview): wire agex overview with claude-code probe"
```

> Snapshot testing via `syrupy` is deferred — content-based assertions are sufficient for v0.1 and avoid `project_dir` path-dependence in snapshots. Add snapshots in a later plan if they become useful.

---

## Phase 5 — `learn` command + first lesson

### Task 18: `commands/learn/` scaffolding + menu template

**Files:**
- Create: `src/agent_experience/commands/learn/__init__.py`
- Create: `src/agent_experience/commands/learn/SKILL.md`
- Create: `src/agent_experience/commands/learn/scripts/__init__.py`
- Create: `src/agent_experience/commands/learn/references/.gitkeep`
- Create: `src/agent_experience/commands/learn/assets/menu.md.j2`

- [ ] **Step 1: Write SKILL.md**

File: `src/agent_experience/commands/learn/SKILL.md`

```markdown
---
name: learn
description: Show available lessons, or teach one.
type: command
---

# `agex learn [topic] --agent <backend>`

Without a topic, lists the lessons available for your backend. With a topic, teaches it — emits a markdown lesson body plus inline skill-template code blocks you can write into your project.

## From your shell tool

```bash
agex learn --agent claude-code
agex learn introspect --agent claude-code
```

## Notes

- Lessons gated on a backend feature (e.g., `gamify` needs hooks) are still listed, marked with an unsupported note.
- v0.1 emits inline code blocks only. A future `--write` flag is tracked as an open issue.
```

- [ ] **Step 2: Write menu template**

File: `src/agent_experience/commands/learn/assets/menu.md.j2`

```jinja
# Lessons — {{ backend }}

Run `agex learn <topic> --agent {{ backend }}` to learn one.

{% for topic in topics %}
- **`{{ topic.name }}`** — {{ topic.description }}
{%- if topic.unsupported %} _(unsupported on {{ backend }}: {{ topic.unsupported }})_{%- endif %}
{% endfor %}
```

- [ ] **Step 3: Init placeholders**

```bash
touch src/agent_experience/commands/learn/__init__.py
touch src/agent_experience/commands/learn/scripts/__init__.py
mkdir -p src/agent_experience/commands/learn/references
touch src/agent_experience/commands/learn/references/.gitkeep
```

- [ ] **Step 4: Commit**

```bash
git add src/agent_experience/commands/learn
git commit -m "feat(learn): scaffold skill-folder with SKILL.md and menu template"
```

---

### Task 19: First lesson — `introspect` skill-folder

**Files:**
- Create: `src/agent_experience/commands/learn/assets/topics/introspect/SKILL.md`
- Create: `src/agent_experience/commands/learn/assets/topics/introspect/assets/skill-template/claude-code/SKILL.md`

- [ ] **Step 1: Write lesson body**

File: `src/agent_experience/commands/learn/assets/topics/introspect/SKILL.md`

```markdown
---
name: introspect
description: Build an agent-native introspect skill that audits your project setup and suggests next steps.
type: lesson
---

# Lesson — build an `introspect` skill for {{ backend }}

## What you'll end up with

A backend-native skill (for {{ backend }}) that:
1. Calls `agex overview --agent {{ backend }}` to read the project's current state.
2. Identifies gaps (missing CLAUDE.md, no skills, no hooks, etc.).
3. Suggests the next one or two improvements you could apply.
4. Is small enough that invoking it doesn't blow your context budget.

## Why build it instead of shipping it

Two reasons: (1) each agent backend has its own native skill format, so no shipped skill fits perfectly; (2) you and the user stay in control of what gets installed into the project.

## Step 1 — review the `agex overview` output

Run `agex overview --agent {{ backend }}` now. Note which sections are empty — those are your candidate gaps.

## Step 2 — create the skill file

Write the file shown below to the path noted above its fence. Adjust the prose/tone to your project's voice.

### `.claude/skills/introspect/SKILL.md`

```markdown
{{ skill_template_body }}
```

## Step 3 — try it

Invoke `/introspect` (or equivalent) in your runtime. Read the output, apply one suggestion.

## Why one suggestion at a time

Agents that shove 10 recommendations into one turn overwhelm the user. The skill you just built is explicitly capped at two — if you want more later, iterate.
```

- [ ] **Step 2: Write Claude Code skill template**

File: `src/agent_experience/commands/learn/assets/topics/introspect/assets/skill-template/claude-code/SKILL.md`

```markdown
---
name: introspect
description: Audit the current project's agent setup and suggest 1-2 next improvements.
type: command
---

# Introspect

When the user asks to audit the agent setup, improve it, or "what should I add", invoke this skill.

## Process

1. Run in your shell tool: `agex overview --agent claude-code`
2. Read the output. Count what exists under each section (skills, hooks, MCP, settings).
3. Identify the two weakest sections — the ones most likely to unblock a real workflow next.
4. Emit a short markdown reply: what's missing, why it matters, what adding it costs.

## Rules

- Cap suggestions at 2.
- No "nice-to-haves." Only suggestions that unblock something concrete.
- Don't install anything. Advise only.
```

- [ ] **Step 3: Commit**

```bash
git add src/agent_experience/commands/learn/assets/topics/introspect
git commit -m "feat(learn): add introspect lesson + claude-code skill template"
```

---

### Task 20: `learn.py` script + CLI wiring + tests

**Files:**
- Create: `src/agent_experience/commands/learn/scripts/learn.py`
- Modify: `src/agent_experience/cli.py`
- Create: `tests/commands/test_learn.py`

- [ ] **Step 1: Write failing tests**

File: `tests/commands/test_learn.py`

```python
from typer.testing import CliRunner

from agent_experience.cli import app


def test_learn_menu_lists_introspect():
    runner = CliRunner()
    result = runner.invoke(app, ["learn", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "introspect" in result.stdout


def test_learn_introspect_emits_lesson_and_template():
    runner = CliRunner()
    result = runner.invoke(app, ["learn", "introspect", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "build an `introspect` skill" in result.stdout
    # Template body embedded as code block
    assert "Audit the current project" in result.stdout


def test_learn_unknown_topic_errors_with_menu():
    runner = CliRunner()
    result = runner.invoke(app, ["learn", "xyz", "--agent", "claude-code"])
    assert result.exit_code == 2
    assert "unknown topic" in result.stderr.lower()
    assert "introspect" in result.stdout  # menu in stdout
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/commands/test_learn.py -v`
Expected: FAIL

- [ ] **Step 3: Implement script**

File: `src/agent_experience/commands/learn/scripts/learn.py`

```python
from importlib.resources import files
from pathlib import Path

from agent_experience.core.backend import Backend
from agent_experience.core.render import render_file, render_string
from agent_experience.core.skill_loader import load_skill


def _topics_root() -> Path:
    return Path(str(files("agent_experience.commands.learn.assets.topics")))


def _menu_template() -> Path:
    return Path(str(files("agent_experience.commands.learn.assets"))) / "menu.md.j2"


def _list_topics() -> list[dict]:
    topics: list[dict] = []
    if not _topics_root().exists():
        return topics
    for topic_dir in sorted(_topics_root().iterdir()):
        skill_md = topic_dir / "SKILL.md"
        if not skill_md.exists():
            continue
        skill = load_skill(skill_md)
        topics.append({"name": skill.name, "description": skill.description, "unsupported": None})
    return topics


def run_menu(backend: Backend) -> tuple[str, int, str]:
    topics = _list_topics()
    out = render_file(_menu_template(), {"backend": backend.value, "topics": topics})
    return (out, 0, "")


def run_topic(topic: str, backend: Backend) -> tuple[str, int, str]:
    topic_dir = _topics_root() / topic
    skill_md = topic_dir / "SKILL.md"
    if not skill_md.exists():
        menu_out, _, _ = run_menu(backend)
        return (menu_out, 2, f"agex: error: unknown topic '{topic}'")

    skill = load_skill(skill_md)
    template_path = (
        topic_dir / "assets" / "skill-template" / backend.value / "SKILL.md"
    )
    template_body = template_path.read_text() if template_path.exists() else ""
    rendered = render_string(
        skill.body,
        {"backend": backend.value, "skill_template_body": template_body},
    )
    return (rendered, 0, "")
```

- [ ] **Step 4: Wire into CLI**

Modify: `src/agent_experience/cli.py` — add after `overview`:

```python
from agent_experience.commands.learn.scripts import learn as learn_script


@app.command("learn")
def learn(
    topic: Optional[str] = typer.Argument(None, help="Lesson topic (omit for menu)."),
    agent: str = _agent_option(),
) -> None:
    try:
        backend = parse_backend(agent)
    except ValueError as e:
        typer.echo(f"agex: error: {e}", err=True)
        raise typer.Exit(code=2)
    if topic is None:
        stdout, exit_code, stderr = learn_script.run_menu(backend)
    else:
        stdout, exit_code, stderr = learn_script.run_topic(topic, backend)
    if stdout:
        typer.echo(stdout, nl=False)
    if stderr:
        typer.echo(stderr, err=True)
    if exit_code != 0:
        raise typer.Exit(code=exit_code)
```

- [ ] **Step 5: Run — expect pass**

Run: `uv run pytest tests/commands/test_learn.py -v`
Expected: PASS (3 tests)

- [ ] **Step 6: Commit**

```bash
git add src/agent_experience/cli.py \
        src/agent_experience/commands/learn/scripts/learn.py \
        tests/commands/test_learn.py
git commit -m "feat(learn): wire menu and topic dispatch with template embedding"
```

---

### Task 21: Remaining v0.1 lessons — visualize, gamify, levelup

**Files:**
- Create: `src/agent_experience/commands/learn/assets/topics/visualize/SKILL.md`
- Create: `src/agent_experience/commands/learn/assets/topics/visualize/assets/skill-template/claude-code/SKILL.md`
- Create: `src/agent_experience/commands/learn/assets/topics/gamify/SKILL.md`
- Create: `src/agent_experience/commands/learn/assets/topics/gamify/assets/skill-template/claude-code/SKILL.md`
- Create: `src/agent_experience/commands/learn/assets/topics/levelup/SKILL.md`
- Create: `src/agent_experience/commands/learn/assets/topics/levelup/assets/skill-template/claude-code/SKILL.md`

- [ ] **Step 1: Write visualize lesson**

File: `src/agent_experience/commands/learn/assets/topics/visualize/SKILL.md`

```markdown
---
name: visualize
description: Build a skill that renders a compact visual of your agent setup.
type: lesson
---

# Lesson — build a `visualize` skill for {{ backend }}

Same shape as introspect, but the output optimizes for *at-a-glance* rather than recommendations. The skill runs `agex overview --agent {{ backend }}`, compresses it into a tight markdown block (counts only, with a token target of < 500 output tokens), and echoes it for situational awareness.

### Skill template — `.claude/skills/visualize/SKILL.md`

```markdown
{{ skill_template_body }}
```
```

File: `src/agent_experience/commands/learn/assets/topics/visualize/assets/skill-template/claude-code/SKILL.md`

```markdown
---
name: visualize
description: At-a-glance snapshot of the project's agent setup — counts only, compact.
type: command
---

# Visualize

When the user says "what do I have", "show the setup", or similar.

## Process

1. `agex overview --agent claude-code`
2. Emit a compact block: `Skills: N · Hooks: M · MCP: K · CLAUDE.md: ✓/✗`
3. Stop. No recommendations, no prose paragraphs.

## Rule

Target < 500 output tokens. If you can't fit the summary, trim further.
```

- [ ] **Step 2: Write gamify lesson** (bundles levelup teaching)

File: `src/agent_experience/commands/learn/assets/topics/gamify/SKILL.md`

```markdown
---
name: gamify
description: Install usage tracking hooks and build a levelup skill to advise the user.
type: lesson
---

# Lesson — set up gamification for {{ backend }}

Two parts:

## Part 1 — install the tracking hooks

Run in your shell tool:

```bash
agex gamify --agent {{ backend }}
```

This writes backend-native hook fragments that call `agex hook write <event>` whenever you use a tool, submit a prompt, or stop. The events land in `.agex/data/`.

To uninstall: `agex gamify --uninstall --agent {{ backend }}`.

## Part 2 — build the `levelup` skill

The hook data is inert without something to surface it. Build the levelup skill described in `agex learn levelup --agent {{ backend }}`, or copy its skill template directly:

### `.claude/skills/levelup/SKILL.md`

```markdown
{{ skill_template_body }}
```

## After both parts

Use your runtime normally for a few sessions. Then invoke `/levelup` — it will read the tracking data via `agex hook read` and advise the user.
```

File: `src/agent_experience/commands/learn/assets/topics/gamify/assets/skill-template/claude-code/SKILL.md`

```markdown
---
name: levelup
description: Read agex usage tracking data and suggest a next-feature-to-learn for the user.
type: command
---

# Level up

Invoke when the user asks for what's next, or opportunistically after a long session.

## Process

1. Run in your shell tool: `agex hook read --agent claude-code`
2. Count occurrences per event type.
3. Pick **one** feature area the user is under-using (e.g., they have MCP servers configured but `post-tool-use` shows zero MCP tool calls).
4. Suggest one concrete next step in 3-4 sentences.

## Rule

One suggestion per invocation. If nothing stands out, say so — don't invent.
```

- [ ] **Step 3: Write levelup lesson (standalone entry point)**

File: `src/agent_experience/commands/learn/assets/topics/levelup/SKILL.md`

```markdown
---
name: levelup
description: Build a skill that reads agex usage data and advises the user.
type: lesson
---

# Lesson — build the `levelup` skill for {{ backend }}

Prerequisite: you've run `agex gamify --agent {{ backend }}` so there's data to read.

### Skill template — `.claude/skills/levelup/SKILL.md`

```markdown
{{ skill_template_body }}
```

See also: `agex learn gamify --agent {{ backend }}` (bundles the full setup).
```

File: `src/agent_experience/commands/learn/assets/topics/levelup/assets/skill-template/claude-code/SKILL.md`

```markdown
---
name: levelup
description: Read agex usage tracking data and suggest a next-feature-to-learn for the user.
type: command
---

# Level up

Invoke when the user asks for what's next, or opportunistically after a long session.

## Process

1. Run in your shell tool: `agex hook read --agent claude-code`
2. Count occurrences per event type.
3. Pick **one** feature area the user is under-using (e.g., they have MCP servers configured but `post-tool-use` shows zero MCP tool calls).
4. Suggest one concrete next step in 3-4 sentences.

## Rule

One suggestion per invocation. If nothing stands out, say so — don't invent.
```

- [ ] **Step 4: Extend `test_learn.py` with coverage**

Append to: `tests/commands/test_learn.py`

```python
def test_learn_menu_lists_all_v01_topics():
    runner = CliRunner()
    result = runner.invoke(app, ["learn", "--agent", "claude-code"])
    for topic in ("introspect", "visualize", "gamify", "levelup"):
        assert topic in result.stdout


def test_learn_visualize_emits_lesson():
    runner = CliRunner()
    result = runner.invoke(app, ["learn", "visualize", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "visualize" in result.stdout.lower()


def test_learn_gamify_includes_levelup_template():
    runner = CliRunner()
    result = runner.invoke(app, ["learn", "gamify", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "gamify" in result.stdout
    assert "levelup" in result.stdout
```

- [ ] **Step 5: Run — expect pass**

Run: `uv run pytest tests/commands/test_learn.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/agent_experience/commands/learn/assets/topics \
        tests/commands/test_learn.py
git commit -m "feat(learn): add visualize, gamify, levelup lessons + templates"
```

---

## Phase 6 — `hook` commands

### Task 22: `commands/hook/` scaffolding + write command + CLI wiring

**Files:**
- Create: `src/agent_experience/commands/hook/__init__.py`
- Create: `src/agent_experience/commands/hook/SKILL.md`
- Create: `src/agent_experience/commands/hook/scripts/__init__.py`
- Create: `src/agent_experience/commands/hook/scripts/write.py`
- Create: `src/agent_experience/commands/hook/scripts/read.py`
- Create: `src/agent_experience/commands/hook/references/.gitkeep`
- Create: `src/agent_experience/commands/hook/assets/table.md.j2`
- Modify: `src/agent_experience/cli.py`
- Create: `tests/commands/test_hook.py`

- [ ] **Step 1: Write SKILL.md**

File: `src/agent_experience/commands/hook/SKILL.md`

```markdown
---
name: hook
description: Write and read agex tracking events.
type: command
---

# `agex hook write <event> [key=value ...]` / `agex hook read --agent <backend>`

## `write`

Called by installed hooks (see `agex gamify`). Appends a JSON line to `.agex/data/<event>.json`. Silent. Safe for concurrent invocation.

```bash
agex hook write post-tool-use tool=Read
```

## `read`

Renders tracked events as a markdown table. Prints the source JSON path for deeper inspection.

```bash
agex hook read --agent claude-code
```

## Notes

- Event names are free-form; conventional names: `post-tool-use`, `user-prompt`, `stop`, `sessions`.
- Extra positional `key=value` pairs are captured into the payload.
- Timestamp is attached automatically if not supplied.
```

- [ ] **Step 2: Write table template**

File: `src/agent_experience/commands/hook/assets/table.md.j2`

```jinja
# Hook events — {{ backend }}

**Source:** `{{ source }}`

{% for stream in streams %}
## `{{ stream.name }}` ({{ stream.events|length }})

{% if stream.events %}
| ts | event | details |
|---|---|---|
{%- for e in stream.events %}
| {{ e.ts }} | {{ stream.name }} | {{ e.details }} |
{%- endfor %}
{% else %}
_no events_
{% endif %}
{% endfor %}
```

- [ ] **Step 3: Write scripts**

File: `src/agent_experience/commands/hook/scripts/write.py`

```python
from datetime import datetime, timezone

from agent_experience.core.hook_io import append_event
from agent_experience.core.paths import ensure_init


def run(event: str, args: list[str]) -> tuple[str, int, str]:
    ensure_init()
    payload: dict = {"ts": datetime.now(tz=timezone.utc).isoformat()}
    for arg in args:
        if "=" in arg:
            k, v = arg.split("=", 1)
            payload[k] = v
    payload["event"] = event
    append_event(event, payload)
    return ("", 0, "")
```

File: `src/agent_experience/commands/hook/scripts/read.py`

```python
from importlib.resources import files
from pathlib import Path

from agent_experience.core.backend import Backend
from agent_experience.core.hook_io import load_events
from agent_experience.core.paths import data_dir, ensure_init
from agent_experience.core.render import render_file


KNOWN_STREAMS = ["post-tool-use", "user-prompt", "stop", "sessions"]


def _table_template() -> Path:
    return Path(str(files("agent_experience.commands.hook.assets"))) / "table.md.j2"


def run(backend: Backend) -> tuple[str, int, str]:
    ensure_init()
    streams = []
    for name in KNOWN_STREAMS:
        events = load_events(name)
        summarized = [
            {
                "ts": e.get("ts", ""),
                "details": ", ".join(f"{k}={v}" for k, v in e.items() if k not in ("ts", "event")),
            }
            for e in events
        ]
        streams.append({"name": name, "events": summarized})

    out = render_file(
        _table_template(),
        {"backend": backend.value, "source": str(data_dir()), "streams": streams},
    )
    return (out, 0, "")
```

- [ ] **Step 4: Wire subcommands into CLI**

Modify: `src/agent_experience/cli.py` — add:

```python
from agent_experience.commands.hook.scripts import write as hook_write_script
from agent_experience.commands.hook.scripts import read as hook_read_script


hook_app = typer.Typer(help="Write and read agex tracking events.", no_args_is_help=True)
app.add_typer(hook_app, name="hook")


@hook_app.command("write")
def hook_write(
    event: str = typer.Argument(..., help="Event name (e.g., post-tool-use)."),
    args: list[str] = typer.Argument(None, help="Additional key=value pairs."),
) -> None:
    args = args or []
    _, exit_code, stderr = hook_write_script.run(event, args)
    if stderr:
        typer.echo(stderr, err=True)
    if exit_code != 0:
        raise typer.Exit(code=exit_code)


@hook_app.command("read")
def hook_read(agent: str = _agent_option()) -> None:
    try:
        backend = parse_backend(agent)
    except ValueError as e:
        typer.echo(f"agex: error: {e}", err=True)
        raise typer.Exit(code=2)
    stdout, exit_code, stderr = hook_read_script.run(backend)
    if stdout:
        typer.echo(stdout, nl=False)
    if stderr:
        typer.echo(stderr, err=True)
    if exit_code != 0:
        raise typer.Exit(code=exit_code)
```

- [ ] **Step 5: Write tests**

File: `tests/commands/test_hook.py`

```python
from typer.testing import CliRunner

from agent_experience.cli import app


def test_hook_write_is_silent_and_creates_file(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    result = runner.invoke(app, ["hook", "write", "post-tool-use", "tool=Read"])
    assert result.exit_code == 0
    assert result.stdout == ""
    assert (tmp_path / ".agex" / "data" / "post-tool-use.json").exists()


def test_hook_read_renders_table_with_source(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    runner.invoke(app, ["hook", "write", "post-tool-use", "tool=Read"])
    runner.invoke(app, ["hook", "write", "post-tool-use", "tool=Write"])
    result = runner.invoke(app, ["hook", "read", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "post-tool-use" in result.stdout
    assert "Source:" in result.stdout
    assert "Read" in result.stdout or "tool=Read" in result.stdout


def test_hook_read_empty_shows_no_events(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    result = runner.invoke(app, ["hook", "read", "--agent", "claude-code"])
    assert result.exit_code == 0
    assert "_no events_" in result.stdout
```

- [ ] **Step 6: Init placeholders**

```bash
touch src/agent_experience/commands/hook/__init__.py
touch src/agent_experience/commands/hook/scripts/__init__.py
mkdir -p src/agent_experience/commands/hook/references
touch src/agent_experience/commands/hook/references/.gitkeep
```

- [ ] **Step 7: Run — expect pass**

Run: `uv run pytest tests/commands/test_hook.py -v`
Expected: PASS (3 tests)

- [ ] **Step 8: Commit**

```bash
git add src/agent_experience/cli.py \
        src/agent_experience/commands/hook \
        tests/commands/test_hook.py
git commit -m "feat(hook): add write and read subcommands with JSON append + markdown table"
```

---

## Phase 7 — `gamify` command + Claude Code hook fragments

### Task 23: `commands/gamify/` scaffolding + Claude-Code hook fragments

**Files:**
- Create: `src/agent_experience/commands/gamify/__init__.py`
- Create: `src/agent_experience/commands/gamify/SKILL.md`
- Create: `src/agent_experience/commands/gamify/scripts/__init__.py`
- Create: `src/agent_experience/commands/gamify/references/.gitkeep`
- Create: `src/agent_experience/commands/gamify/assets/hooks/claude-code.json`

- [ ] **Step 1: Write SKILL.md**

File: `src/agent_experience/commands/gamify/SKILL.md`

```markdown
---
name: gamify
description: Install or uninstall backend-native hooks that track usage via agex hook write.
type: command
---

# `agex gamify --agent <backend>` / `agex gamify --uninstall --agent <backend>`

## What it does

Writes backend-native hook fragments (each tagged with a stable `agex:*` ID) that call `agex hook write <event>` on PostToolUse, UserPromptSubmit, and Stop events. Agent-authored skills (e.g., `levelup`) read the accumulated data via `agex hook read`.

## Why it's safe

- Idempotent: re-running is a no-op.
- Reversible: `--uninstall` removes exactly the `agex:*` fragments; user-authored hooks are untouched.
- Calling `agex gamify` explicitly is the confirmation — no separate prompt.

## Unsupported backends

If your backend doesn't support hooks, you get a markdown notice + issue link instead.

## From your shell tool

```bash
agex gamify --agent claude-code
# ... use your runtime for a while ...
agex hook read --agent claude-code
# ... later, to undo:
agex gamify --uninstall --agent claude-code
```
```

- [ ] **Step 2: Write Claude Code hook fragments**

File: `src/agent_experience/commands/gamify/assets/hooks/claude-code.json`

```json
{
  "fragments": [
    {
      "id": "agex:post-tool-use",
      "event": "PostToolUse",
      "hook": {
        "type": "command",
        "command": "agex hook write post-tool-use tool=$CLAUDE_TOOL_NAME"
      }
    },
    {
      "id": "agex:user-prompt",
      "event": "UserPromptSubmit",
      "hook": {
        "type": "command",
        "command": "agex hook write user-prompt"
      }
    },
    {
      "id": "agex:stop",
      "event": "Stop",
      "hook": {
        "type": "command",
        "command": "agex hook write stop"
      }
    }
  ]
}
```

- [ ] **Step 3: Init placeholders**

```bash
touch src/agent_experience/commands/gamify/__init__.py
touch src/agent_experience/commands/gamify/scripts/__init__.py
mkdir -p src/agent_experience/commands/gamify/references
touch src/agent_experience/commands/gamify/references/.gitkeep
```

- [ ] **Step 4: Commit**

```bash
git add src/agent_experience/commands/gamify
git commit -m "feat(gamify): scaffold skill-folder + claude-code hook fragments"
```

---

### Task 24: `install.py` — install + uninstall + idempotency

**Files:**
- Create: `src/agent_experience/commands/gamify/scripts/install.py`
- Modify: `src/agent_experience/cli.py`
- Create: `tests/commands/test_gamify.py`

- [ ] **Step 1: Write failing tests**

File: `tests/commands/test_gamify.py`

```python
import json
from datetime import datetime, timezone
from pathlib import Path

from typer.testing import CliRunner

from agent_experience.cli import app
from agent_experience.core.config import load as load_config


def test_gamify_install_writes_hooks_and_config(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    result = runner.invoke(app, ["gamify", "--agent", "claude-code"])
    assert result.exit_code == 0

    hooks_file = tmp_path / ".claude" / "hooks.json"
    assert hooks_file.exists()
    data = json.loads(hooks_file.read_text())
    assert "PostToolUse" in data
    assert any(h["id"] == "agex:post-tool-use" for h in data["PostToolUse"])

    cfg = load_config()
    assert "gamify" in cfg.installed
    assert sorted(cfg.installed["gamify"]["hook_fragment_ids"]) == [
        "agex:post-tool-use",
        "agex:stop",
        "agex:user-prompt",
    ]


def test_gamify_install_idempotent(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    runner.invoke(app, ["gamify", "--agent", "claude-code"])
    first = (tmp_path / ".claude" / "hooks.json").read_text()
    result = runner.invoke(app, ["gamify", "--agent", "claude-code"])
    assert result.exit_code == 0
    second = (tmp_path / ".claude" / "hooks.json").read_text()
    assert first == second


def test_gamify_install_preserves_user_hooks(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    claude_dir = tmp_path / ".claude"
    claude_dir.mkdir()
    (claude_dir / "hooks.json").write_text(json.dumps({
        "PostToolUse": [{"id": "user:custom", "hook": {"type": "command", "command": "echo hi"}}]
    }))
    runner = CliRunner()
    runner.invoke(app, ["gamify", "--agent", "claude-code"])
    data = json.loads((claude_dir / "hooks.json").read_text())
    ids = [h["id"] for h in data["PostToolUse"]]
    assert "user:custom" in ids
    assert "agex:post-tool-use" in ids


def test_gamify_uninstall_removes_only_agex_fragments(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    claude_dir = tmp_path / ".claude"
    claude_dir.mkdir()
    (claude_dir / "hooks.json").write_text(json.dumps({
        "PostToolUse": [{"id": "user:custom", "hook": {"type": "command", "command": "echo hi"}}]
    }))
    runner = CliRunner()
    runner.invoke(app, ["gamify", "--agent", "claude-code"])
    result = runner.invoke(app, ["gamify", "--uninstall", "--agent", "claude-code"])
    assert result.exit_code == 0
    data = json.loads((claude_dir / "hooks.json").read_text())
    ids = [h["id"] for h in data["PostToolUse"]]
    assert ids == ["user:custom"]

    cfg = load_config()
    assert "gamify" not in cfg.installed
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/commands/test_gamify.py -v`
Expected: FAIL

- [ ] **Step 3: Implement**

File: `src/agent_experience/commands/gamify/scripts/install.py`

```python
import json
from datetime import datetime, timezone
from importlib.resources import files
from pathlib import Path

from agent_experience.core.backend import Backend
from agent_experience.core.config import load as load_config, save as save_config
from agent_experience.core.paths import ensure_init


def _fragments_for(backend: Backend) -> list[dict]:
    if backend != Backend.CLAUDE_CODE:
        return []
    path = Path(str(files("agent_experience.commands.gamify.assets.hooks"))) / "claude-code.json"
    data = json.loads(path.read_text())
    return data["fragments"]


def _hooks_file_for(backend: Backend, project_dir: Path) -> Path | None:
    if backend == Backend.CLAUDE_CODE:
        return project_dir / ".claude" / "hooks.json"
    return None


def _load_hooks_file(path: Path) -> dict:
    if not path.exists():
        return {}
    try:
        return json.loads(path.read_text())
    except json.JSONDecodeError:
        return {}


def _write_hooks_file(path: Path, data: dict) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(data, indent=2) + "\n")


def install(backend: Backend) -> tuple[str, int, str]:
    ensure_init()
    project_dir = Path.cwd()
    hooks_file = _hooks_file_for(backend, project_dir)
    if hooks_file is None:
        return (_unsupported_notice(backend), 0, "")

    fragments = _fragments_for(backend)
    if not fragments:
        return (_unsupported_notice(backend), 0, "")

    hooks = _load_hooks_file(hooks_file)
    written_ids: list[str] = []
    for frag in fragments:
        event = frag["event"]
        entry = {"id": frag["id"], "hook": frag["hook"]}
        hooks.setdefault(event, [])
        if not any(e.get("id") == frag["id"] for e in hooks[event]):
            hooks[event].append(entry)
        written_ids.append(frag["id"])
    _write_hooks_file(hooks_file, hooks)

    cfg = load_config()
    cfg.installed["gamify"] = {
        "at": datetime.now(tz=timezone.utc).isoformat(),
        "hook_fragment_ids": written_ids,
    }
    save_config(cfg)

    lines = [
        f"# Gamify installed — {backend.value}",
        "",
        f"- Wrote {len(written_ids)} hook fragment(s) to `{hooks_file.relative_to(project_dir)}`.",
        "- Fragment IDs: " + ", ".join(f"`{i}`" for i in written_ids),
        "",
        f"Next: run `agex learn gamify --agent {backend.value}` to set up the levelup skill.",
        "",
    ]
    return ("\n".join(lines), 0, "")


def uninstall(backend: Backend) -> tuple[str, int, str]:
    ensure_init()
    project_dir = Path.cwd()
    hooks_file = _hooks_file_for(backend, project_dir)
    if hooks_file is None:
        return (_unsupported_notice(backend), 0, "")

    cfg = load_config()
    installed = cfg.installed.get("gamify", {})
    ids_to_remove = set(installed.get("hook_fragment_ids", []))
    if not ids_to_remove:
        return (f"# Gamify uninstalled — nothing to remove on {backend.value}.\n", 0, "")

    hooks = _load_hooks_file(hooks_file)
    for event in list(hooks.keys()):
        hooks[event] = [e for e in hooks[event] if e.get("id") not in ids_to_remove]
        if not hooks[event]:
            del hooks[event]
    _write_hooks_file(hooks_file, hooks)

    cfg.installed.pop("gamify", None)
    save_config(cfg)

    return (
        f"# Gamify uninstalled — removed {len(ids_to_remove)} fragment(s) from `{hooks_file.relative_to(project_dir)}`.\n",
        0,
        "",
    )


def _unsupported_notice(backend: Backend) -> str:
    return (
        f"## `gamify` is not supported on {backend.value}\n\n"
        f"Hooks are required to track usage events, and {backend.value} does not expose "
        f"a hook interface agex can write to.\n\n"
        "Want this supported? Open an issue: <https://github.com/OriNachum/agent-experience/issues>\n"
    )
```

- [ ] **Step 4: Wire into CLI**

Modify: `src/agent_experience/cli.py` — add:

```python
from agent_experience.commands.gamify.scripts import install as gamify_script


@app.command("gamify")
def gamify(
    agent: str = _agent_option(),
    uninstall: bool = typer.Option(False, "--uninstall", help="Reverse gamify."),
) -> None:
    try:
        backend = parse_backend(agent)
    except ValueError as e:
        typer.echo(f"agex: error: {e}", err=True)
        raise typer.Exit(code=2)
    if uninstall:
        stdout, exit_code, stderr = gamify_script.uninstall(backend)
    else:
        stdout, exit_code, stderr = gamify_script.install(backend)
    if stdout:
        typer.echo(stdout, nl=False)
    if stderr:
        typer.echo(stderr, err=True)
    if exit_code != 0:
        raise typer.Exit(code=exit_code)
```

- [ ] **Step 5: Run — expect pass**

Run: `uv run pytest tests/commands/test_gamify.py -v`
Expected: PASS (4 tests)

- [ ] **Step 6: Commit**

```bash
git add src/agent_experience/cli.py \
        src/agent_experience/commands/gamify/scripts/install.py \
        tests/commands/test_gamify.py
git commit -m "feat(gamify): add install/uninstall with idempotency and user-hook preservation"
```

---

## Phase 8 — Other backends (stubs)

### Task 25: Minimal probes + capability YAMLs for codex, copilot, acp

**Files:**
- Create: `src/agent_experience/backends/codex/__init__.py`
- Create: `src/agent_experience/backends/codex/probe.py`
- Create: `src/agent_experience/backends/copilot/__init__.py`
- Create: `src/agent_experience/backends/copilot/probe.py`
- Create: `src/agent_experience/backends/acp/__init__.py`
- Create: `src/agent_experience/backends/acp/probe.py`
- Create: `src/agent_experience/commands/overview/assets/backends/codex.yaml`
- Create: `src/agent_experience/commands/overview/assets/backends/copilot.yaml`
- Create: `src/agent_experience/commands/overview/assets/backends/acp.yaml`
- Create: `src/agent_experience/backends/capabilities/claude-code.yaml`
- Create: `src/agent_experience/backends/capabilities/codex.yaml`
- Create: `src/agent_experience/backends/capabilities/copilot.yaml`
- Create: `src/agent_experience/backends/capabilities/acp.yaml`
- Create: `tests/backends/test_stub_probes.py`

- [ ] **Step 1: Write stub probes**

File: `src/agent_experience/backends/codex/probe.py`

```python
from pathlib import Path

from agent_experience.backends.claude_code.probe import ProbeResult


def probe(project_dir: Path) -> ProbeResult:
    """Minimal Codex probe — reads AGENTS.md if present. Other discovery deferred."""
    result = ProbeResult()
    if not project_dir.exists():
        return result
    agents_md = project_dir / "AGENTS.md"
    if agents_md.exists():
        result.claude_md = agents_md  # reusing field — rename to `project_memory` in a future cleanup
    return result
```

File: `src/agent_experience/backends/copilot/probe.py`

```python
from pathlib import Path

from agent_experience.backends.claude_code.probe import ProbeResult


def probe(project_dir: Path) -> ProbeResult:
    """Stub Copilot probe — v0.1 returns empty. Full discovery tracked as open issue."""
    return ProbeResult()
```

File: `src/agent_experience/backends/acp/probe.py`

```python
from pathlib import Path

from agent_experience.backends.claude_code.probe import ProbeResult


def probe(project_dir: Path) -> ProbeResult:
    """Stub ACP probe — v0.1 returns empty. Full discovery tracked as open issue."""
    return ProbeResult()
```

File: `src/agent_experience/backends/codex/__init__.py` (empty)

File: `src/agent_experience/backends/copilot/__init__.py` (empty)

File: `src/agent_experience/backends/acp/__init__.py` (empty)

- [ ] **Step 2: Register probes in overview**

Modify: `src/agent_experience/commands/overview/scripts/overview.py` — expand `_PROBES`:

```python
from agent_experience.backends.claude_code.probe import probe as claude_code_probe
from agent_experience.backends.codex.probe import probe as codex_probe
from agent_experience.backends.copilot.probe import probe as copilot_probe
from agent_experience.backends.acp.probe import probe as acp_probe

_PROBES = {
    Backend.CLAUDE_CODE: claude_code_probe,
    Backend.CODEX: codex_probe,
    Backend.COPILOT: copilot_probe,
    Backend.ACP: acp_probe,
}
```

- [ ] **Step 3: Write backend capability YAMLs**

File: `src/agent_experience/backends/capabilities/claude-code.yaml`

```yaml
hooks: true
mcp: true
skills: true
agents: true
```

File: `src/agent_experience/backends/capabilities/codex.yaml`

```yaml
hooks: false
hooks_alternative: |
  Codex does not expose a hook interface. Wrap your Codex invocation in a shell
  script that calls `agex hook write` before/after each turn as a workaround.
mcp: true
skills: false
agents: false
```

File: `src/agent_experience/backends/capabilities/copilot.yaml`

```yaml
hooks: false
hooks_alternative: |
  GitHub Copilot CLI does not expose a hook interface in v0.1-era versions.
  Track usage via a shell wrapper that calls `agex hook write`.
mcp: false
skills: false
agents: false
```

File: `src/agent_experience/backends/capabilities/acp.yaml`

```yaml
hooks: false
hooks_alternative: |
  ACP-speaking agents (KiroCLI, OpenCode) do not currently expose hooks.
  Track usage via the ACP transport layer or a shell wrapper.
mcp: true
skills: false
agents: false
```

- [ ] **Step 4: Write backend-specific overview YAMLs** (mostly symmetric)

Content for each of `codex.yaml`, `copilot.yaml`, `acp.yaml` under `commands/overview/assets/backends/`:

```yaml
sections:
  - project_memory
  - skills
  - hooks
  - mcp
  - settings
probe: <backend_name>
```

- [ ] **Step 5: Write probe smoke tests**

File: `tests/backends/test_stub_probes.py`

```python
from pathlib import Path

from agent_experience.backends.codex.probe import probe as codex_probe
from agent_experience.backends.copilot.probe import probe as copilot_probe
from agent_experience.backends.acp.probe import probe as acp_probe


def test_codex_probe_empty_on_empty_project(tmp_path):
    assert codex_probe(tmp_path).skills == []


def test_codex_probe_reads_agents_md(tmp_path):
    (tmp_path / "AGENTS.md").write_text("# AGENTS\n")
    result = codex_probe(tmp_path)
    assert result.claude_md is not None


def test_copilot_probe_empty(tmp_path):
    assert copilot_probe(tmp_path).skills == []


def test_acp_probe_empty(tmp_path):
    assert acp_probe(tmp_path).skills == []
```

- [ ] **Step 6: Extend gamify test for unsupported path**

Append to: `tests/commands/test_gamify.py`

```python
def test_gamify_codex_emits_unsupported_notice(tmp_path, monkeypatch):
    monkeypatch.chdir(tmp_path)
    runner = CliRunner()
    result = runner.invoke(app, ["gamify", "--agent", "codex"])
    assert result.exit_code == 0
    assert "not supported on codex" in result.stdout.lower()
    assert "github.com" in result.stdout
    assert not (tmp_path / ".claude").exists()
```

- [ ] **Step 7: Init placeholders + run tests**

```bash
touch src/agent_experience/backends/{codex,copilot,acp}/__init__.py
mkdir -p src/agent_experience/backends/capabilities
```

Run: `uv run pytest tests/backends tests/commands/test_gamify.py -v`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add src/agent_experience/backends \
        src/agent_experience/commands/overview/assets/backends \
        tests/backends/test_stub_probes.py \
        tests/commands/test_gamify.py
git commit -m "feat(backends): add minimal codex/copilot/acp probes + capability YAMLs"
```

---

## Phase 9 — Error handling + consistency meta-test

### Task 26: Unknown-command routes to `agex explain agex`

**Files:**
- Modify: `src/agent_experience/cli.py`
- Create: `tests/test_cli_errors.py`

- [ ] **Step 1: Write failing tests**

File: `tests/test_cli_errors.py`

```python
from typer.testing import CliRunner

from agent_experience.cli import app


def test_unknown_command_emits_agex_page_and_exits_2():
    runner = CliRunner()
    result = runner.invoke(app, ["frobnicate"])
    assert result.exit_code == 2
    assert "agex" in result.stdout
    # Content should mention the commands list
    assert "overview" in result.stdout
    assert "learn" in result.stdout
    assert "unknown command" in result.stderr.lower()
```

- [ ] **Step 2: Run — expect fail**

Run: `uv run pytest tests/test_cli_errors.py -v`
Expected: FAIL (Typer default error message differs)

- [ ] **Step 3: Implement custom no-match handler**

Modify: `src/agent_experience/cli.py` — wrap `app()` with a fallback. Replace the `__main__` entry (add if missing):

```python
def _main_entrypoint() -> None:
    import sys

    known_commands = {"explain", "overview", "learn", "gamify", "hook"}
    argv = sys.argv[1:]
    if argv and not argv[0].startswith("-") and argv[0] not in known_commands:
        from agent_experience.commands.explain.scripts import explain as explain_script

        typer.echo(f"agex: error: unknown command '{argv[0]}'", err=True)
        stdout, _, _ = explain_script.run("agex")
        typer.echo(stdout, nl=False)
        sys.exit(2)
    app()


if __name__ == "__main__":
    _main_entrypoint()
```

Modify `pyproject.toml` entry point to use wrapper:

```toml
[project.scripts]
agex = "agent_experience.cli:_main_entrypoint"
```

Re-install: `uv pip install -e ".[dev]"`

- [ ] **Step 4: Adjust test to invoke via subprocess** (CliRunner bypasses argv)

Rewrite: `tests/test_cli_errors.py`

```python
import subprocess
import sys


def test_unknown_command_emits_agex_page_and_exits_2():
    result = subprocess.run(
        [sys.executable, "-m", "agent_experience", "frobnicate"],
        capture_output=True,
        text=True,
    )
    assert result.returncode == 2
    assert "agex" in result.stdout
    assert "overview" in result.stdout
    assert "unknown command" in result.stderr.lower()
```

Also create: `src/agent_experience/__main__.py`

```python
from agent_experience.cli import _main_entrypoint

if __name__ == "__main__":
    _main_entrypoint()
```

- [ ] **Step 5: Run — expect pass**

Run: `uv run pytest tests/test_cli_errors.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/agent_experience/cli.py \
        src/agent_experience/__main__.py \
        pyproject.toml \
        tests/test_cli_errors.py
git commit -m "feat(cli): route unknown commands to agex explain agex with exit 2"
```

---

### Task 27: SKILL.md consistency meta-test

**Files:**
- Create: `tests/test_skill_md_consistency.py`

- [ ] **Step 1: Write the meta-test**

File: `tests/test_skill_md_consistency.py`

```python
from importlib.resources import files
from pathlib import Path

import pytest

from agent_experience.core.skill_loader import load_skill


def _all_skill_md_paths() -> list[Path]:
    commands_root = Path(str(files("agent_experience.commands")))
    return sorted(commands_root.glob("**/SKILL.md"))


@pytest.mark.parametrize("skill_path", _all_skill_md_paths(), ids=str)
def test_skill_md_has_valid_frontmatter(skill_path: Path):
    skill = load_skill(skill_path)
    assert skill.name
    assert skill.description
    assert skill.type in ("command", "lesson")


def test_every_command_has_skill_md():
    commands_root = Path(str(files("agent_experience.commands")))
    expected = {"explain", "overview", "learn", "gamify", "hook"}
    for cmd in expected:
        assert (commands_root / cmd / "SKILL.md").exists(), f"{cmd}/SKILL.md missing"
```

- [ ] **Step 2: Run — expect pass** (everything should already comply)

Run: `uv run pytest tests/test_skill_md_consistency.py -v`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add tests/test_skill_md_consistency.py
git commit -m "test: enforce SKILL.md frontmatter consistency across commands and lessons"
```

---

## Phase 10 — Docs site (Jekyll + Cloudflare Pages)

### Task 28: Docs site scaffolding

**Files:**
- Create: `docs/_config.yml`
- Create: `docs/Gemfile`
- Create: `docs/index.md`
- Create: `docs/getting-started.md`
- Create: `docs/.gitignore`

- [ ] **Step 1: Copy styling from culture**

```bash
cp -r /home/spark/git/culture/_sass docs/_sass
cp -r /home/spark/git/culture/_includes docs/_includes
```

(If those paths don't exist because culture's structure differs, use `just-the-docs` as a fallback theme; spec notes this as acceptable.)

- [ ] **Step 2: Write `_config.yml`**

File: `docs/_config.yml`

```yaml
title: agent-experience
description: Agent-operated developer-experience CLI.
url: https://agent-experience.culture.dev
baseurl: ""

# Theme fallback — replace with culture's when _sass is wired
theme: just-the-docs

markdown: kramdown
permalink: pretty

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
```

- [ ] **Step 3: Write `Gemfile`**

File: `docs/Gemfile`

```ruby
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.7"
```

- [ ] **Step 4: Write landing page**

File: `docs/index.md`

```markdown
---
title: agent-experience
layout: default
nav_order: 1
---

# agent-experience

Non-agentic Python CLI that emits deterministic per-backend markdown for autonomous agents.

- **Install:** `uv tool install agent-experience` or `pipx install agent-experience`
- **First command:** `agex explain agex`
- **Repo:** <https://github.com/OriNachum/agent-experience>

See [Getting started](getting-started.html) for the quickstart.
```

- [ ] **Step 5: Write getting-started page**

File: `docs/getting-started.md`

```markdown
---
title: Getting started
layout: default
nav_order: 2
---

# Getting started

```bash
uv tool install agent-experience
agex explain agex
agex overview --agent claude-code
agex learn --agent claude-code
```

## As an agent

You almost never run `agex` yourself. Your human installs it and an agent-authored skill wraps each command. Start with `agex learn introspect --agent <your-backend>`.
```

- [ ] **Step 6: .gitignore**

File: `docs/.gitignore`

```
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
Gemfile.lock
```

- [ ] **Step 7: Test local build**

Run: `cd docs && bundle install && bundle exec jekyll build`
Expected: `_site/` created, no errors.

- [ ] **Step 8: Commit**

```bash
git add docs/
git commit -m "docs: scaffold Jekyll site with landing and getting-started pages"
```

---

### Task 29: Auto-import SKILL.md files into docs/

**Files:**
- Create: `scripts/sync_skill_md.py`
- Modify: `docs/_config.yml` (add `commands` to nav)

- [ ] **Step 1: Write sync script**

File: `scripts/sync_skill_md.py`

```python
"""Copy command SKILL.md files into docs/commands/ for Jekyll rendering."""

from pathlib import Path
import shutil
import sys

ROOT = Path(__file__).parent.parent
COMMANDS = ROOT / "src" / "agent_experience" / "commands"
DOCS_COMMANDS = ROOT / "docs" / "commands"


def main() -> int:
    DOCS_COMMANDS.mkdir(parents=True, exist_ok=True)
    order = 10
    for cmd_dir in sorted(COMMANDS.iterdir()):
        skill_md = cmd_dir / "SKILL.md"
        if not skill_md.exists():
            continue
        content = skill_md.read_text()
        # Swap frontmatter for Jekyll-friendly one
        body = content.split("---\n", 2)[-1]
        new_frontmatter = (
            "---\n"
            f"title: {cmd_dir.name}\n"
            "layout: default\n"
            "parent: Commands\n"
            f"nav_order: {order}\n"
            "---\n\n"
        )
        out = DOCS_COMMANDS / f"{cmd_dir.name}.md"
        out.write_text(new_frontmatter + body)
        order += 10

    # Index page
    (DOCS_COMMANDS / "index.md").write_text(
        "---\ntitle: Commands\nlayout: default\nnav_order: 3\nhas_children: true\n---\n\n"
        "# Commands\n\n"
        "Auto-imported from `src/agent_experience/commands/*/SKILL.md`.\n"
    )
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Run locally to verify**

Run: `python scripts/sync_skill_md.py`
Expected: `docs/commands/{explain,overview,learn,gamify,hook}.md` created.

- [ ] **Step 3: Rebuild Jekyll to verify**

Run: `cd docs && bundle exec jekyll build`
Expected: `_site/commands/` contains the generated pages.

- [ ] **Step 4: Commit**

```bash
git add scripts/sync_skill_md.py docs/commands/
git commit -m "docs: add SKILL.md auto-sync script and generated command pages"
```

---

### Task 30: Cloudflare Pages deploy workflow

**Files:**
- Create: `.github/workflows/docs.yml`

- [ ] **Step 1: Write workflow**

File: `.github/workflows/docs.yml`

```yaml
name: docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "src/agent_experience/commands/**/SKILL.md"
      - "scripts/sync_skill_md.py"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python scripts/sync_skill_md.py
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler-cache: true
          working-directory: docs
      - run: cd docs && bundle exec jekyll build
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: agent-experience
          directory: docs/_site
          branch: main
```

> Cloudflare Pages project creation and custom-domain binding (`agent-experience.culture.dev`) is a one-time manual step tracked as a GitHub issue.

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/docs.yml
git commit -m "ci: add docs build and Cloudflare Pages deploy workflow"
```

---

## Phase 11 — `tester-agents/claude/`

### Task 31: Claude tester workspace

**Files:**
- Create: `tester-agents/claude/CLAUDE.md`
- Create: `tester-agents/claude/culture.yaml`
- Create: `tester-agents/claude/.claude/settings.json`
- Create: `tester-agents/claude/README.md`
- Create: `tester-agents/claude/.claude/skills` — **symlink** to `../../../src/agent_experience/commands`

- [ ] **Step 1: Create workspace files**

File: `tester-agents/claude/CLAUDE.md`

```markdown
# CLAUDE.md — agex-tester (claude)

You are an **agex dogfooding agent**. Your job is to exercise every `agex` command end-to-end from a Claude Code runtime and surface UX issues.

## Persona

You speak briefly, report findings as short markdown bullets, and do not improvise fixes — you file findings.

## Your skills

`.claude/skills/` is a symlink to the shipped `commands/` folder of `agent-experience`. Each command is available as a skill. Invoke them verbatim to test the real surface.

## Your tasks (in order)

1. `agex explain agex` — self-describing page.
2. `agex overview --agent claude-code` — snapshot of this workspace.
3. `agex learn --agent claude-code` — menu.
4. `agex learn introspect --agent claude-code` — full lesson.
5. `agex gamify --agent claude-code` — install hooks. Then use a tool; then `agex hook read --agent claude-code`.
6. `agex gamify --uninstall --agent claude-code` — reverse.

## Reporting

For each test, one short bullet:
- Pass / fail
- Output length in characters
- Anything weird

Humans on the culture mesh can ping you (`@<server>-agex-tester-claude test overview`) to repeat the relevant step.
```

File: `tester-agents/claude/culture.yaml`

```yaml
nick: agex-tester-claude
backend: claude
description: agex dogfooding agent for Claude Code runtime.
```

File: `tester-agents/claude/.claude/settings.json`

```json
{
  "permissions": {
    "allow": ["Bash(agex:*)"]
  }
}
```

File: `tester-agents/claude/README.md`

```markdown
# agex-tester (claude)

Culture-connected agent that dogfoods `agex` end-to-end.

## Register

```bash
culture agent register tester-agents/claude/
culture start <server>-agex-tester-claude
```

## Symlink note

`.claude/skills/` is a symlink to `../../../src/agent_experience/commands/`. On Windows, directory symlinks require elevated privileges — see the spec for the known limitation.
```

- [ ] **Step 2: Create the symlink**

Run:

```bash
mkdir -p tester-agents/claude/.claude
cd tester-agents/claude/.claude
ln -s ../../../src/agent_experience/commands skills
cd -
```

Verify: `ls -la tester-agents/claude/.claude/` shows `skills -> ../../../src/agent_experience/commands`.

- [ ] **Step 3: Verify resolution**

Run: `ls tester-agents/claude/.claude/skills/overview/SKILL.md`
Expected: the file resolves through the symlink and opens.

- [ ] **Step 4: Commit**

```bash
git add tester-agents/
git commit -m "feat(tester-agents): add claude dogfooding workspace with symlink to commands/"
```

---

## Phase 12 — Publishing workflow

### Task 32: Publish workflow for PyPI + TestPyPI

**Files:**
- Create: `.github/workflows/publish.yml`

- [ ] **Step 1: Write workflow**

File: `.github/workflows/publish.yml`

```yaml
name: publish

on:
  push:
    branches: [main]
    tags: ["v*"]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: uv venv
      - run: uv pip install -e ".[dev]" build
      - run: uv run python -m build
      - id: meta
        run: |
          python -c "import tomllib; print('version=' + tomllib.load(open('pyproject.toml','rb'))['project']['version'])" >> $GITHUB_OUTPUT
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish-testpypi:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

  publish-pypi:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/publish.yml
git commit -m "ci: add publish workflow for TestPyPI (main) and PyPI (tags)"
```

---

### Task 33: Final smoke pass

- [ ] **Step 1: Run full test suite**

Run: `uv run pytest`
Expected: all tests pass across all files.

- [ ] **Step 2: Run all lint/format tools**

```bash
uv run black src tests
uv run isort src tests
uv run flake8 src tests
uv run bandit -r src
```

Expected: all clean or expected warnings documented.

- [ ] **Step 3: Update CHANGELOG**

Modify: `CHANGELOG.md` — move `[Unreleased]` content under `[0.1.0] - 2026-04-18` heading.

- [ ] **Step 4: Tag release**

```bash
git add CHANGELOG.md
git commit -m "chore: release v0.1.0"
git tag -a v0.1.0 -m "agex v0.1.0"
```

> **Note:** Do not push the tag until the user explicitly asks. The publish workflow triggers on `v*` tags.

- [ ] **Step 5: Report plan complete**

Tell the user: "v0.1.0 implementation complete. Tests passing. Ready to push `main` and (separately, when you confirm) tag `v0.1.0` to trigger PyPI publish."

---

## Open issues to file after merge

Per spec — create GitHub issues for each:

1. Codex tester workspace under `tester-agents/codex/`.
2. GitHub Copilot CLI tester workspace.
3. KiroCLI (ACP) tester workspace.
4. OpenCode (ACP) tester workspace.
5. `agex learn <topic> --write` flag for direct skill-file emission.
6. `agex config` user-facing command.
7. Additional lesson topics (`migrate`, `onboard`, `mcp-setup`).
8. Custom Jinja filters when concrete need arises.
9. Cloudflare Pages project creation + `agent-experience.culture.dev` domain binding.
10. Re-style `docs/` with culture's exact `_sass`/`_includes` (currently falls back to `just-the-docs`).
